<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
	프로젝트 관련 쿼리 모음.

	[페이지네이션 쿼리 구조]
	목록 조회는 아래 3중 구조로 되어 있다. 바깥에서 안쪽 순서로 읽으면 된다.

	  SELECT * FROM (                                   ③ sub  : 잘라낸 결과
	      SELECT ROW_NUMBER() OVER(...) AS rnum, list.*  ② rnum : 정렬된 행에 1부터 번호를 매김
	      FROM ( ... 실제 조회 + 검색조건 ... ) list      ① list : 조건에 맞는 전체 결과
	  ) sub
	  WHERE rnum 초과 startRowNumber, rnum 이하 endRowNumber

	rnum 범위는 (startRowNumber, endRowNumber] — 시작값은 제외, 끝값은 포함이다.
	두 값은 Criteria 가 계산한다. 1페이지면 (0, 10] 이 되어 1~10번 행이 나온다.

	원래 Oracle 의 ROWNUM 을 쓰던 코드로, PostgreSQL 전환 시 ROW_NUMBER() OVER() 로 바꿨다.
	LIMIT/OFFSET 으로 단순화할 수도 있지만 Java 쪽 Criteria 를 함께 고쳐야 해서 구조를 유지했다.

	[주의] 검색 조건의 ${searchType} 은 #{} 가 아니라 ${} 라서 값이 SQL 에 그대로 박힌다.
	       컬럼명을 동적으로 넣기 위한 것이지만 외부 입력이 그대로 들어오면 SQL 인젝션이 되므로,
	       searchType 은 반드시 화면에서 고른 정해진 값만 전달되어야 한다.
-->
<mapper namespace="com.bnc.mapper.ProjectMapper">

	<!--
		내 회사가 발주(req) 또는 수주(acp)한 프로젝트의 전체 건수.
		목록 쿼리(selectMyProjectList)와 검색 조건이 정확히 같아야 페이지 수가 어긋나지 않는다.
	-->
	<select id="selectMyProjectListCount" parameterType="hashmap" resultType="int">
		<![CDATA[
		SELECT count(*) FROM bnc_project
		WHERE (proj_req_biznum = #{companyData.cmpy_biznum} OR proj_acp_biznum = #{companyData.cmpy_biznum})
		]]>
		<choose>
			<when test="pageData.searchType != null and pageData.searchType !='' ">
			AND ${pageData.searchType} like '%'||#{pageData.searchKeyword}||'%'
			</when>
		</choose>
	</select>

	<!-- 내 회사 프로젝트 목록 (페이지 단위). 구조 설명은 파일 상단 주석 참고. -->
	<select id="selectMyProjectList" parameterType="hashmap" resultType="com.bnc.domain.ProjectVO">
		SELECT * FROM (
			SELECT ROW_NUMBER() OVER(ORDER BY proj_number DESC) AS rnum, list.* FROM (
				<![CDATA[
				SELECT * FROM bnc_project
				WHERE (proj_req_biznum = #{companyData.cmpy_biznum} OR proj_acp_biznum = #{companyData.cmpy_biznum})
				]]>
				<choose>
					<when test="pageData.searchType != null and pageData.searchType !='' ">
						AND ${pageData.searchType} like '%'||#{pageData.searchKeyword}||'%'
					</when>
				</choose>
				ORDER BY proj_number DESC
			) list
		) sub
		<![CDATA[WHERE rnum > #{pageData.startRowNumber} AND rnum <= #{pageData.endRowNumber}]]>
	</select>

	<!-- 전체 프로젝트 건수 (모집 목록용). 위 selectMyProjectListCount 와 달리 회사 조건이 없다. -->
	<select id="selectProjectListCount" resultType="int">
		<![CDATA[
		SELECT count(*) FROM bnc_project
		]]>
		<choose>
			<when test="searchType != null and searchType !='' ">
			WHERE bnc_project.${searchType} like '%'||#{searchKeyword}||'%'
			</when>
		</choose>
	</select>

	<!-- 전체 프로젝트 목록 (페이지 단위). 구조 설명은 파일 상단 주석 참고. -->
	<select id="selectProjectList" resultType="com.bnc.domain.ProjectVO">
		SELECT * FROM (
			SELECT ROW_NUMBER() OVER(ORDER BY proj_number DESC) AS rnum, list.* FROM (
				SELECT * FROM bnc_project project
				<choose>
					<when test="searchType != null and searchType !='' ">
						WHERE project.${searchType} like '%'||#{searchKeyword}||'%'
					</when>
				</choose>
				ORDER BY proj_number DESC
			) list
		) sub
		<![CDATA[WHERE rnum > #{startRowNumber} AND rnum <= #{endRowNumber}]]>
	</select>

	<update id="updateProjectMatching">
		UPDATE bnc_project
		SET
				proj_acp_phone 	= #{prpl_acp_phone},
				proj_acp_biznum = #{prpl_acp_biznum},
				proj_flag		= 'C'
		WHERE 	proj_number 	= #{prpl_number}
	</update>

	<update id="updateProjectFlag">
		UPDATE bnc_project
		SET
				proj_flag		= #{proj_flag}
		WHERE 	proj_number 	= #{proj_number}
	</update>

	<select id="selectProjectParticipateData" resultType="com.bnc.domain.ProjectParticipateVO">
		SELECT * FROM
			bnc_project_participate_list
		WHERE prpl_number 	= #{prpl_number}
		AND prpl_acp_biznum = #{prpl_acp_biznum}
	</select>

	<select id="selectProjectParticipateList" resultType="com.bnc.domain.ProjectParticipateVO">
		SELECT * FROM
			bnc_project_participate_list pplist, bnc_company company
		WHERE pplist.prpl_acp_biznum = company.cmpy_biznum
		AND pplist.prpl_number = #{projectNumber}
	</select>

	<select id="selectProjectParticipateCheckCount" resultType="int">
		SELECT COUNT(*) FROM
			bnc_project_participate_list
		WHERE prpl_number 		= #{prpl_number}
		AND prpl_acp_biznum 	= #{prpl_acp_biznum}
	</select>

	<insert id="insertProjectParticipateData">
		INSERT INTO bnc_project_participate_list
		(
			prpl_number,
			prpl_acp_biznum,
			prpl_acp_price,
			prpl_acp_phone,
			prpl_file_path,
			prpl_file_name,
			prpl_rdate
		)
		VALUES(
			#{prpl_number},
			#{prpl_acp_biznum},
			#{prpl_acp_price},
			#{prpl_acp_phone},
			#{prpl_file_path, 	jdbcType=VARCHAR},
			#{prpl_file_name, 	jdbcType=VARCHAR},
			TO_CHAR(NOW(), 'YYYYMMDDHH24MISS')
		)
	</insert>

	<select id="selectProjectView" resultType="com.bnc.domain.ProjectVO">
		SELECT * FROM
			bnc_company company,
			bnc_project project
		WHERE
			company.cmpy_biznum = project.proj_req_biznum
		AND project.proj_number = #{projectNumber}
	</select>

	<update id="updateProjectData">
		UPDATE bnc_project

		SET
			<if test="proj_name != null">proj_name = #{proj_name, 	jdbcType=VARCHAR},</if>
			<if test="proj_kind != null">proj_kind = #{proj_kind, 	jdbcType=VARCHAR},</if>
			<if test="proj_req_phone != null">proj_req_phone = #{proj_req_phone, 	jdbcType=VARCHAR},</if>
			<if test="proj_acp_phone != null">proj_acp_phone = #{proj_acp_phone, 	jdbcType=VARCHAR},</if>
			<if test="proj_acp_biznum != null">proj_acp_biznum = #{proj_acp_biznum, 	jdbcType=VARCHAR},</if>
			<if test="proj_mov_url != null">proj_mov_url = #{proj_mov_url, 	jdbcType=VARCHAR},</if>
			<if test="proj_keyword != null">proj_keyword = #{proj_keyword, 	jdbcType=VARCHAR},</if>
			<if test="proj_lead_time != null">proj_lead_time = #{proj_lead_time, 	jdbcType=VARCHAR},</if>
			<if test="proj_flag != null">proj_flag = #{proj_flag, 	jdbcType=VARCHAR},</if>
			<if test="proj_price_range != null">proj_price_range = #{proj_price_range, 	jdbcType=VARCHAR},</if>
			<if test="proj_thumb_file_path != null">proj_thumb_file_path = #{proj_thumb_file_path, 	jdbcType=VARCHAR},</if>
			<if test="proj_contents != null">proj_contents = #{proj_contents, 	jdbcType=VARCHAR},</if>
			<if test="proj_req_flag != null">proj_req_flag = #{proj_req_flag, 	jdbcType=VARCHAR},</if>
			<if test="proj_acp_flag != null">proj_acp_flag = #{proj_acp_flag, 	jdbcType=VARCHAR},</if>

			proj_udate 			= TO_CHAR(NOW(), 'YYYYMMDDHH24MISS')

		where proj_number = #{proj_number}
	</update>

	<insert id="insertProjectData">
		<selectKey keyProperty="proj_number" resultType="int" order="BEFORE">
			select nextval('bnc_project_seq')
		</selectKey>
		insert into BNC_PROJECT
		(
			proj_number,
			proj_name,
			proj_kind,
			proj_req_phone,
			proj_req_biznum,
			proj_acp_phone,
			proj_acp_biznum,
			proj_mov_url,
			proj_keyword,
			proj_lead_time,
			proj_flag,
			proj_price_range,
			proj_thumb_file_path,
			proj_contents,
			proj_rdate,
			proj_udate
		)
		values
		(
			#{proj_number},
			#{proj_name},
			#{proj_kind},
			#{proj_req_phone},
			#{proj_req_biznum},
			#{proj_acp_phone, 	jdbcType=VARCHAR},
			#{proj_acp_biznum, 	jdbcType=VARCHAR},
			#{proj_mov_url, 	jdbcType=VARCHAR},
			#{proj_keyword, 	jdbcType=VARCHAR},
			#{proj_lead_time, 	jdbcType=VARCHAR},
			'W',
			#{proj_price_range},
			#{proj_thumb_file_path, jdbcType=VARCHAR},
			#{proj_contents},
			TO_CHAR(NOW(), 'YYYYMMDDHH24MISS'),
			TO_CHAR(NOW(), 'YYYYMMDDHH24MISS')
		)
	</insert>


	<!-- 파일 인서트 -->
	<insert id="insertProjectFiles">
		insert into bnc_project_file
		(prof_seq, prof_proj_number, prof_file_path, prof_file_name)
		values(nextval('bnc_project_file_seq'), #{prof_proj_number}, #{prof_file_path}, #{prof_file_name})
	</insert>


	<update id="updateProjectWorkingFlag">
		UPDATE bnc_project
		<choose>
			<when test="authCheck != null and authCheck =='req' ">
				SET proj_req_flag	= #{proj_req_flag}
			</when>
			<when test="authCheck != null and authCheck =='acp' ">
				SET proj_acp_flag	= #{proj_acp_flag}
			</when>
		</choose>
		WHERE 	proj_number 	= #{proj_number}
	</update>

	<select id="selectProjectFiles" resultType="com.bnc.domain.ProjectFileDTO">
		select * from bnc_project_file
		where prof_proj_number = #{projectNumber}
		order by prof_seq asc
	</select>

	<select id="projectCount" resultType="int">
		select count(*) from bnc_project
	</select>

	<!-- 프로젝트 신청 기업 삭제 -->
	<delete id="deleteProjectParticipateData">
		delete from bnc_project_participate_list
		WHERE prpl_number = #{proj_number}
	</delete>

	<!-- 프로젝트 삭제 -->
	<delete id="deleleProject">
		delete from bnc_project
		where proj_number = #{proj_number}
	</delete>

	<!-- 프로젝트 파일 전체 삭제 -->
	<delete id="deleteProjectFile">
		delete from bnc_project_file
		where prof_proj_number = #{proj_number}
	</delete>

	<!-- 프로젝트 파일 선택 삭제 -->
	<delete id="deleteProjectFilesNotIn" parameterType="java.util.HashMap">
		DELETE FROM bnc_project_file
		WHERE prof_seq NOT IN
		<foreach collection="prof_files" item="item" index="index" separator="," open="(" close=")">
        #{item}
		</foreach>
		AND prof_proj_number = #{proj_number}
	</delete>

</mapper>
